{
  "name": "musicstats",
  "version": "1.0.0",
  "description": "Apple Music stats tracker — rastrea minutos, artistas y canciones",
  "main": "api/config.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
